jar {
  baseName = 'task-manage'
}

configurations {
  sqlScripts
}

dependencies {
  sqlScripts files('../../../infra/orchestrate/ansible/roles/task-manage_db_migrate/files/scripts')
}

eclipse {
  classpath {
    plusConfigurations += [ configurations.sqlScripts ]
  }
}
