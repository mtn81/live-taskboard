jar {
  baseName = 'task-manage'
}

configurations {
  sqlScripts
}

dependencies {
  sqlScripts files('../../../vm/orchestrate/ansible/roles/task-manage_db_migrate/files/scripts')
}

eclipse {
  classpath {
    plusConfigurations += [ configurations.sqlScripts ]
  }
}
